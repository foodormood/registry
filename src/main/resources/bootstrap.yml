spring:
  profiles:
    active: dev
  application:
    name: registry
  cloud:
    config:
      uri: http://127.0.0.1:8888
      fail-fast: false
      password: 11223344
      username: config-server
      label: main

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
